protocol CaseIterable2 { }

{% for enum in types.enums.implementing.CaseIterable2 %}
extension {{ enum.name }} {
	static let allCases: [{{ enum.name }}] = [
	{% for case in enum.cases %} .{{ case.name }}
	{% endfor %}]
}
{% endfor %}
